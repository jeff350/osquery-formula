# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: osquery
  rootgroup: root
  config: '/etc/osquery'
  service:
    name: osquery
  subcomponent:
    config: '/etc/osquery-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
